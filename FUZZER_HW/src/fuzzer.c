#include "fuzzer.h"

int run_fuzzer(FILE *seed_file, int job_count, int input_count, int time_limit, char *target_program[]) {
    (void) seed_file;
    (void) job_count;
    (void) input_count;
    (void) time_limit;
    (void) target_program;
    return 0;
}